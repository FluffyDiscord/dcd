# Secrets for the side containers (mariadb, meilisearch), in Docker env-file
# format. Committed empty so the example validates in a fresh checkout; on a real
# target this file carries the values and is never committed.
