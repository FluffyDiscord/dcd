# Secrets for the app, scheduler and every release container, in Docker env-file
# format (KEY=value, no quoting, no interpolation) — NOT the dcd dotenv chain.
# Committed empty so the example validates in a fresh checkout; on a real target
# this file carries the values and is never committed.
