name: images

# Tags only. A branch push publishes nothing: every image this repository ships is
# addressable by an immutable version, so a consumer can never be silently moved onto
# a different build.
on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  binary:
    name: binary (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      # Built inside Alpine so the musl target is native for this architecture. The
      # container is driven from the host because Alpine cannot host JS actions on arm64.
      - name: Build and test the static binary
        run: |
          docker run --rm -v "$PWD":/build -w /build rust:1-alpine sh -eu -c '
            apk add --no-cache musl-dev build-base
            cargo build --release --locked
            cargo test --locked
            ./target/release/dcd --version
          '

      - uses: actions/upload-artifact@v4
        with:
          name: dcd-${{ matrix.arch }}
          path: target/release/dcd
          if-no-files-found: error
          retention-days: 1

  images:
    name: image (${{ matrix.flavor }})
    needs: binary
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: debian
            base: debian:stable-slim
            suffix: ''
          - flavor: alpine
            base: alpine:3
            suffix: '-alpine'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          pattern: dcd-*
          path: artifacts

      - name: Lay out the per-architecture binaries
        run: |
          mkdir -p dist/amd64 dist/arm64
          install -m 0755 artifacts/dcd-amd64/dcd dist/amd64/dcd
          install -m 0755 artifacts/dcd-arm64/dcd dist/arm64/dcd

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          # NO MOVING TAGS. `latest` is off permanently: metadata-action's `latest=auto`
          # marks any non-prerelease semver as latest without comparing versions, so
          # re-pushing an OLD tag silently repoints it at an older build - and anything
          # pulling `latest` gets downgraded without a config change. `{{major}}.{{minor}}`
          # and `edge` move for the same reason and are gone with it.
          flavor: |
            latest=false
            suffix=${{ matrix.suffix }}
          tags: |
            type=semver,pattern={{version}}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          build-args: BASE_IMAGE=${{ matrix.base }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
